import GenreSelect from "../components/GenreSelect";
export default {
  title: "Genre Select",
  component: GenreSelect,
};

const Template = (args) => <GenreSelect />;

export const Default = Template.bind({});
